{
	"schemaVersion": 1,
	"id": "tmp-spec",
	"version": "${version}",
	"name": "tmp spec",
	"description": "Allow everyone to access spectator mode without allowing them to use it to replace travel.\nAnyone in survival as access to the /specswap command, once run they will be put into spectator mode. The next time they run it they will be put right back to where they were when they entered spectator mode.",
	"authors": [
		"jSdCool"
	],
	"contact": {
		"homepage": "https://github.com/jSdCool/tmp-spec",
		"sources": "https://github.com/jSdCool/tmp-spec"
	},
	"license": "CC0-1.0",
	"icon": "assets/tmp-spec/icon.png",
	"environment": "*",
	"entrypoints": {
		"main": [
			"com.cbi.tmpspec.TmpSpec"
		]
	},
	"mixins": [
		"tmp-spec.mixins.json",
		{
			"config": "tmp-spec.client.mixins.json",
			"environment": "client"
		}
	],
	"depends": {
		"fabricloader": ">=0.18.4",
		"minecraft": ">=26.1",
		"java": ">=25",
		"fabric-api": "*"
	},
	"suggests": {
		"another-mod": "*"
	}
}